export const enum Key {
    attributes,
    connected_callback,
    constructor,
    constructor_insert_index,
    connected_callback_insert_index,
    need_getter,
    need_setter,
    needs_ref,
    setter_metadata,
    shadow_root,
    static_html,
    tag_name,
}
